# FreeBSD 6.0-RELEASE ia64 硬件兼容列表

- 原文链接：[FreeBSD/ia64 6.0-RELEASE Hardware Notes](https://www.freebsd.org/releases/6.0R/hardware-ia64/)

**The FreeBSD Documentation Project**

版权所有 © 2000, 2001, 2002, 2003, 2004, 2005 The FreeBSD Documentation Project

```ini
$FreeBSD: src/release/doc/en_US.ISO8859-1/hardware/ia64/article.sgml,v 1.1.2.6 2005/10/24 20:19:59 rnoland Exp $
```

FreeBSD 是 FreeBSD 基金会的注册商标。

Intel、Celeron、EtherExpress、i386、i486、Itanium、Pentium 和 Xeon 是 Intel Corporation 或其子公司在美国及其他国家的商标或注册商标。

许多制造商和销售商用来区分其产品的名称被主张为商标。凡是在本文件中出现这些名称，并且 FreeBSD 项目知晓其商标主张时，这些名称后均标注了“™”或“®”符号。

>**摘要**
>
> 这是一个初步文档。它不完整，需要补充内容。请将有关 IA-64 处理器、主板以及在 FreeBSD 上工作的各种设备的更多信息发送至 [FreeBSD IA64 移植邮件列表](http://lists.FreeBSD.org/mailman/listinfo/freebsd-ia64)。

## 1 引言

本文档包含 FreeBSD 6.0-RELEASE 在 IA-64 硬件平台（也称为 FreeBSD/ia64 6.0-RELEASE）上的硬件兼容性说明。它列出了已知在此平台上工作的设备，以及一些在尝试为新设备配置支持时可能有用的引导时内核自定义说明。

>**注意**
>
> 本文档包含特定于 IA-64 硬件平台的信息。其他架构的硬件兼容性说明在某些细节上会有所不同。

有关 FreeBSD/ia64 的更多信息，请参阅 [FreeBSD/ia64 项目](http://www.FreeBSD.org/platforms/ia64/index.html)页面。

## 2 支持的处理器和主板

当前支持的处理器是 [Itanium](http://people.freebsd.org/~marcel/refs/ia64/itanium/24532003.pdf) 和 [Itanium 2](http://people.freebsd.org/~marcel/refs/ia64/itanium2/25111003.pdf)。

支持的芯片组包括：

- HP zx1
- Intel 460GX
- Intel E8870

单处理器（UP）和对称多处理器（SMP）配置均受支持。

### 2.1 支持的设备概述

可在 ia64 机器中找到或与之兼容的大多数设备都完全受支持。显著的例外是 VGA 控制台。FreeBSD 对 VGA 控制台的支持目前过多地基于 PC 硬件，且并非所有 ia64 机器都拥有提供足够 PC 传统支持的芯片组。因此，无法启用 [syscons(4)](http://www.freebsd.org/cgi/man.cgi?query=syscons&sektion=4&manpath=FreeBSD+6.0-RELEASE)，需要使用串行控制台。

## 3 支持的设备

本节描述当前已知 FreeBSD 在 IA-64 平台上支持的设备。其他配置也可能工作，但尚未经过测试。欢迎提供此列表的反馈、更新和更正。

在可能的情况下，列出了适用于每个设备或设备类的驱动程序。如果相关驱动程序在 FreeBSD 基本发行版中有手册页（大多数应该有），则在此处引用。有关受支持的特定设备、控制器等的详细信息，可在手册页中找到。

>**注意**
>
> 本文档中的设备列表是从 FreeBSD 手册页自动生成的。这意味着某些受多个驱动程序支持的设备可能会出现多次。

### 3.1 磁盘控制器

FreeBSD 支持多种磁盘控制器，包括 IDE/ATA 控制器（[ata(4)](http://www.freebsd.org/cgi/man.cgi?query=ata&sektion=4&manpath=FreeBSD+6.0-RELEASE) 驱动程序）和各种 SCSI/SATA/RAID 控制器。

完整的受支持磁盘控制器列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.2 以太网接口

FreeBSD 支持多种以太网接口。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.3 无线网络接口

FreeBSD 支持多种无线网络接口。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.4 串行接口

FreeBSD 支持多种串行接口。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.5 音频设备

FreeBSD 支持多种音频设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.6 USB 设备

FreeBSD 支持多种 USB 设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.7 IEEE 1394 (Firewire) 设备

FreeBSD 支持 IEEE 1394 (Firewire) 设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.8 蓝牙设备

FreeBSD 支持蓝牙设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.9 加密加速器

FreeBSD 支持加密加速器设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

### 3.10 其他设备

FreeBSD 还支持各种其他设备。完整的受支持设备列表，请参阅[英文原文](https://www.freebsd.org/releases/6.0R/hardware-ia64/)。

---

此文件及其他与发布相关的文档可从 [ftp://ftp.FreeBSD.org/](ftp://ftp.freebsd.org/) 下载。

如有关于 FreeBSD 的问题，请先阅读[文档](http://www.freebsd.org/docs.html)，再联系 <[questions@FreeBSD.org](mailto:questions@FreeBSD.org)>。

如有关于此文档的问题，请发送电子邮件至 <[doc@FreeBSD.org](mailto:doc@FreeBSD.org)>。
